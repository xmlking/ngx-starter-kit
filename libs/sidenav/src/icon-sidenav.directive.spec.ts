import { IconSidenavDirective } from './icon-sidenav.directive';

describe('IconSidenavDirective', () => {
  it('should create an instance', () => {
    const directive = new IconSidenavDirective();
    expect(directive).toBeTruthy();
  });
});
